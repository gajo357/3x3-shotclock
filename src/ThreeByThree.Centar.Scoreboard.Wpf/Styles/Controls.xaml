<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <FontFamily x:Key="AppFontFamily">../Assets/Fonts/#Roboto Condensed</FontFamily>
    <FontFamily x:Key="ScoreboardScoreFontFamily">../Assets/Fonts/#Saira</FontFamily>
    <FontFamily x:Key="ScoreboardTimingFontFamily">../Assets/Fonts/#Rajdhani</FontFamily>

    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource BlackBrush}" />
        <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PanelRaisedBrush}" />
        <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource GreyBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="16,10" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="MinHeight" Value="48" />
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource PanelRaisedBrush}" />
        <Setter Property="Foreground" Value="{StaticResource WhiteBrush}" />
        <Setter Property="CaretBrush" Value="{StaticResource WhiteBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource DisabledBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="17" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="Margin" Value="4" />
    </Style>

    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{StaticResource GreyBrush}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Padding" Value="4,8,4,2" />
    </Style>
</ResourceDictionary>
