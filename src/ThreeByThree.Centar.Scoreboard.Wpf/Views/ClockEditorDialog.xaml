<Window x:Class="ThreeByThree.Centar.Scoreboard.Wpf.Views.ClockEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="440"
        Height="260"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <UniformGrid Columns="3">
            <StackPanel>
                <Label HorizontalAlignment="Center" Content="MINUTES" />
                <TextBox HorizontalContentAlignment="Center"
                         FontSize="28"
                         Text="{Binding Minutes, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel>
                <Label HorizontalAlignment="Center" Content="SECONDS" />
                <TextBox HorizontalContentAlignment="Center"
                         FontSize="28"
                         Text="{Binding Seconds, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
            <StackPanel>
                <Label HorizontalAlignment="Center" Content="TENTHS" />
                <TextBox HorizontalContentAlignment="Center"
                         FontSize="28"
                         Text="{Binding Tenths, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </UniformGrid>

        <StackPanel Grid.Row="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button IsCancel="True" Content="CANCEL" />
            <Button IsDefault="True" Click="OnApply" Content="APPLY" />
        </StackPanel>
    </Grid>
</Window>
